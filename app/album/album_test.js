'use strict';

describe('myApp.album module', function() {

  beforeEach(module('myApp.album'));

  describe('album controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var albumController = $controller('albumController');
      expect(albumController).toBeDefined();
    }));

  });
});